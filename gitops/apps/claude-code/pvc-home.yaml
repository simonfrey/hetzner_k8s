apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: claude-home
  namespace: claude-code
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: hcloud-volumes
  resources:
    requests:
      storage: 1Gi
