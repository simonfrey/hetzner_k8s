apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: windows-os-disk
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: hcloud-volumes-nbg1
  resources:
    requests:
      storage: 50Gi
