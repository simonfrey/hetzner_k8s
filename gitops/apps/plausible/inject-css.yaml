apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: inject-css
  namespace: plausible
spec:
  plugin:
    rewritebody:
      rewrites:
        - regex: "</head>"
          replacement: "<style>main + div{display:none;}</style></head>"
